export function Footer() {
  return (
    <footer className="h-9 border-t border-border/70 bg-card/85 backdrop-blur-sm flex items-center justify-between px-4 text-[11px] text-muted-foreground shrink-0">
      <span className="tracking-wide">AI output is assistive, not authoritative. Validate against official filings and portfolio policy.</span>
      <span className="hidden sm:inline font-mono text-[10px] uppercase tracking-[0.12em]">Fund Assistant Workspace</span>
    </footer>
  );
}
